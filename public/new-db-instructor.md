จัดให้ตามคำขอครับ! สรุปโครงสร้างระบบ E-commerce ฉบับสมบูรณ์ที่เน้นความเรียบง่ายแต่ "เอาอยู่" สำหรับโปรเจคเริ่มต้นจนถึงระดับกลาง พร้อมรายละเอียดการจัดการ **Order Timestamps** ที่คุณเน้นมาครับ

---

## 🛒 E-commerce System Architecture Summary

### 🖼️ การจัดการรูปภาพ (Image Strategy)

เราจะใช้แนวทาง **Hybrid Approach** เพื่อความประหยัดและรวดเร็ว

* **Physical Files:** เก็บไว้ที่ `/public/products/` ในโปรเจค Next.js
* **Database Reference:** เก็บเป็น `String` (Path) เช่น `/products/iphone-15.png`
* **Rendering:** ใช้คอมโพเนนต์ `<Image />` ของ Next.js เพื่อทำ Image Optimization (WebP, Lazy Load) อัตโนมัติ

---

### 🗄️ โครงสร้างฐานข้อมูล (PostgreSQL Schema)

แนะนำให้แบ่ง Table เพื่อความยืดหยุ่น (Scalability) ดังนี้:

#### 1. ตารางสินค้าและหมวดหมู่

| Table | Fields ที่สำคัญ | คำอธิบาย |
| --- | --- | --- |
| **Categories** | `id`, `name`, `slug` | หมวดหมู่สินค้า |
| **Products** | `id`, `name`, `description`, `price`, `stock`, `category_id` | ข้อมูลหลักของสินค้า |
| **Product_Images** | `id`, `product_id`, `image_url`, `sort_order` | 1 สินค้ามีได้หลายรูป |

#### 2. ตารางการสั่งซื้อ (หัวใจของระบบ)

| Table | Fields ที่สำคัญ | คำอธิบาย |
| --- | --- | --- |
| **Orders** | `id`, `customer_name`, `phone`, `address`, `total_price`, **`created_at`** | ข้อมูลหัวราการสั่งซื้อ |
| **Order_Items** | `id`, `order_id`, `product_id`, `quantity`, `price_at_purchase` | รายการสินค้าใน Order นั้นๆ |

---

## 🕒 การจัดการเวลาสั่งซื้อ (Order Timestamps)

หัวข้อนี้สำคัญมากเพื่อให้คุณรู้ว่า "ใคร สั่งอะไร เมื่อไหร่" และใช้ในการสรุปยอดขายครับ

### 1. การเก็บข้อมูล (Storing)

ใน PostgreSQL ให้ใช้ Data Type เป็น `TIMESTAMP WITH TIME ZONE` (TIMESTAMPTZ) และตั้งค่า Default ให้เป็นเวลาปัจจุบันเสมอ

```sql
-- ตัวอย่างโครงสร้างตาราง Order
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    total_price DECIMAL(10, 2),
    -- เก็บเวลาที่สร้าง record อัตโนมัติ
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP 
);

```

> **ทำไมต้อง TIMESTAMPTZ?** เพื่อป้องกันปัญหาเวลาเพี้ยนเมื่อคุณย้าย Server หรือดูข้อมูลจาก Timezone ที่ต่างกัน ระบบจะปรับเวลาให้ตรงตามมาตรฐานสากล (UTC) เสมอครับ

### 2. การแสดงผล (Displaying)

เวลาดึงข้อมูลมาโชว์ที่หน้าบ้าน (Frontend) เราควรแปลงให้เป็น Format ที่คนไทยอ่านง่าย โดยใช้ **Intl.DateTimeFormat** หรือ Library อย่าง **dayjs**

**ตัวอย่างโค้ด Next.js (Frontend):**

```javascript
const orderDate = new Date(order.created_at);

// แสดงผลแบบ: 24 ก.พ. 2026, 14:30
const formattedDate = orderDate.toLocaleString('th-TH', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
  hour: '2-digit',
  minute: '2-digit',
});

return <span>สั่งซื้อเมื่อ: {formattedDate}</span>;

```

---

## 🚀 แผนภาพความสัมพันธ์ (ER-Diagram Concept)

* **User** (1) ----> (*) **Orders**
* **Orders** (1) ----> (*) **Order_Items**
* **Products** (1) ----> (*) **Order_Items**
* **Products** (1) ----> (*) **Product_Images**

---

## 💡 ข้อแนะนำเพิ่มเติมสำหรับ "Combined Checkout"

เนื่องจากคุณจะทำหน้า Checkout รวม (กรอกที่อยู่ + เลือกสินค้า + จ่ายเงิน ในที่เดียว):

1. **Server Action:** เมื่อกดปุ่มสั่งซื้อ ให้ส่ง `Cart State` และ `Form Data` ไปที่ Server Action ชุดเดียว
2. **Transaction:** ในฝั่ง DB ให้ใช้ `Transaction` เพื่อให้มั่นใจว่าถ้าสร้าง Order สำเร็จ แต่สร้าง Order_Items ล้มเหลว ระบบจะทำการ Rollback (ยกเลิกทั้งหมด) เพื่อไม่ให้ข้อมูลขยะค้างในระบบ
3. **Stock Validation:** อย่าลืมเช็คสต็อกวินาทีสุดท้ายก่อนบันทึกลงฐานข้อมูลด้วยนะ!
